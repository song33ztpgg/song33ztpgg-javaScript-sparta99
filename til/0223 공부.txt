멀티스레드 : 하나의 프로그램 내에서 여러 작업을 수행
객체지향 :프로그램을 개발하는 방법론
프로그램 작동방식 
하나의 프로세스(실행된 프로그램) 실행
 ->운영체제로 부터(메모리,연산능령)할당 
->프로그램이 연속적 프로세스 실행

프로세스 : 실행중인 컴퓨터 프로그램,운영체제로 부터 시스템 자원을 받는다
프로그래밍 언어 : 프로세스가 수행할 코드
스레드 : 하나의 프로세스 흐름 단위 

연산량,구조적 시간 걸리는것 :렌더, 통신

 자바스크립트 엔진은 싱글 스레드로 동작하지만, 비동기 처리를 하기 위해 있는 별도의 스레드들이 있다
자바스크립트 런타임은 싱글 스레드가 아니다

promise.then( ) 
결과값을 받을때 사용

promise.catch( ) 
명령어가 실패했을때만 실행 

promise.finally() 
성공하든 실패하든 반드시 실행 됨 

.all 
프로미스의 모든 작업이 이행될때 까지 기다린다 
일부 작업만 수행하게 되면 오류를 발생

.race 
프로미스의 모든 작업중 가장 빨리 이행된것을 실행됩니다
나머지 작업의 성공,실패는 무시합니다

resolve(value) — 일이 성공적으로 끝난 경우 그 결과를 나타내는 value와 함께 호출
reject(error) — 에러 발생 시 에러 객체를 나타내는 error와 함께 호출









Promise.allSettled(promises) – 최근에 추가된 메서드로 모든 프라미스가 처리될 때까지 기다렸다가 그 결과(객체)를 담은 배열을 반환합니다. 객체엔 다음과 같은 정보가 담깁니다.
status: "fulfilled" 또는 "rejected"
value(프라미스가 성공한 경우) 또는 reason(프라미스가 실패한 경우)